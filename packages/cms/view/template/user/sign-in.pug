extends ../../layout/signed-out

block append head
    title=view.translate(locale, 'user.sign_in.title')
    meta(name="description" content=view.translate(locale, 'user.sign_in.description'))

block content

    h1.h2=view.translate(locale, 'user.sign_in.headline')

    .typecast-text
        p=view.translate(locale, 'user.sign_in.text')

    +error(errors=form.errors)

    +form(method='post' form=form)
        .form-group
            label(for='email')=view.translate(locale, 'form.label.email')
            +textfield.form-control(id='email' type='email' name='email' form=form required='required')

        .form-group
            label(for='password')=view.translate(locale, 'typecast.form.label.password')
            +textfield.form-control(id='password' type='password' name='password' form=form)

        button.btn.btn-primary(type='submit')=view.translate(locale, 'typecast.form.sign_in')

    .typecast-text
        p!=view.translate(locale, 'typecast.user.sign_in.text_bottom', {url: view.url('/typecast/user/password-reset')})
